<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecowas_facts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ecowas_transparent"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ecowas_transparent"
        android:supportsRtl="true"
        android:theme="@style/Theme.LanguageFacts">
        <activity android:name="com.example.ecowas_facts.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.ecowas_facts.DetailActivity" />
        <activity android:name="com.example.ecowas_facts.MainActivity"/>
    </application>

</manifest>